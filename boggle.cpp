#include <iostream>
#include <string>

using namespace std;

int main()
{
    int total;
    cin >> totalCase;
    char[5][5] boggle;
    int i = 0;
    while(i < totalCase){
        cin >> char[i];
        i++;
    }
    int wordsNum;
    cin >> wordsNum;
    string[] words = new string[wordsNum];
    for(int j = 0; j < wordsNum; j++){
        cin >> words[j];
    }
    bool[] results = new bool[wordsNum];
    for(int k = 0; k < wordsNum; k++)
    {
        if (hasWord(string& str, int x, int y);
    }
    return 0;
}
